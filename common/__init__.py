"""Package marker for the sibling `common` package.

This file is intentionally empty; it makes `common` a package so
`import common.connect_mysql` works when running modules from the
project root.
"""

print("common包被导入了！")